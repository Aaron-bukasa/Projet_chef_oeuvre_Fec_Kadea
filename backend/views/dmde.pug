extends layout
  
block content
    h1 Validation de la demande
    div.validation-demande
        form(action="#", method="POST")
            p Demande de : strong Jean Dupont
            p Titre de la demande : strong Demande de subvention
            p Date de la demande : strong 2023-03-23
            p État de la demande : strong En cours de validation
            hr
            p Veuillez valider la demande :
            div
                label(for="validation") Validation
                select(id="validation", name="validation")
                    option(value="") Choisir
                    option(value="valider") Valider
                    option(value="refuser") Refuser
            div
                label(for="commentaire") Commentaire (facultatif)
                textarea(id="commentaire", name="commentaire", rows="5", cols="50")
            button(type="submit") Valider

    link(rel="stylesheet", href="/stylesheets/dmde.css")
